# Fuji-Fy Shared UI

Reusable React Native components shared between mobile and desktop wrappers.

Components:

- `BeforeAfterCompare`: draggable split compare view.
- `PresetEditor`: preset quick-switch and key control editor.

